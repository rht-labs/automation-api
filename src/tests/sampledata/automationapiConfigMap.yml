#Sample YAML config file

kind: ConfigMap
apiVersion: v1
metadata:
  name: automationapi
data:
  application.yml: |-
    automation:
      global:
        default:
          pagesize: 10
    automationapi:
      message: openshift enabled!
    spring:
      datasource:
        url: jdbc:postgresql://172.30.22.174:5432/automation
        username: autoautoauto
        password: g155LeB6VFqipp6X
      jpa:
        properties:
          hibernate:
            default_schema: automation
        hibernate:
          ddl-auto: create-drop
    logging:
      level:
        org:
          hibernate:
            SQL: debug
